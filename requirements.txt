torch>=2.1
numpy>=1.24
scipy>=1.10
pyyaml>=6.0
pytest>=7.4

# Optional -- only needed if wiring in real pretrained encoders or a real
# document corpus instead of this repo's offline synthetic data/models.
# sentence-transformers>=2.2
# rasterio>=1.3
